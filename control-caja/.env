# Tags
tagAplicativo="$(aplicativo)"
tagArea="$(area)"
tagCelula="$(celula)"
tagCompania="$(compania)"
tagDisponibilidad="$(disponibilidad)"
tagProducto="$(producto)"
tagProyecto="$(proyecto)"

# Common
deploymentBucketName="$(deploymentBucketName)"
project="$(project)"
region="$(region)"

# Specific
analyticsAccountIdDEV="$(analyticsAccountIdDEV)"
analyticsAccountIdUAT="$(analyticsAccountIdUAT)"
analyticsAccountIdPDN="$(analyticsAccountIdPDN)"
arnKmsDEV="$(arnKmsDEV)"
arnKmsUAT="$(arnKmsUAT)"
arnKmsPDN="$(arnKmsPDN)"
jobNumberOfWorkersDEV="$(jobNumberOfWorkersDEV)"
jobNumberOfWorkersUAT="$(jobNumberOfWorkersUAT)"
jobNumberOfWorkersPDN="$(jobNumberOfWorkersPDN)"
chronosBucketName="$(chronosBucketName)"
datafoundationBucket="$(datafoundationBucket)"
extraPythonFilesControlCaja="$(extraPythonFilesControlCaja)"
glueDatabase="$(glueDatabase)"
glueJobVersion="$(glueJobVersion)"
prefixData="$(prefixData)"
prefixFiles="$(prefixFiles)"
projectBucket="$(projectBucket)"
projectBucketJobsPath="$(projectBucketJobsPath)"

# Databases
rtbcolDatabase="$(rtbcolDatabase)"
unityDatabase="$(unityDatabase)"
trmDatabase="$(trmDatabase)"
dominusDatabase="$(dominusDatabase)"
mambuCcDatabase="$(mambuCcDatabase)"
checkingAccountsDatabase="$(checkingAccountsDatabase)"
adapterDatabase="$(adapterDatabase)"

# Tables
unityOperationsTable="$(unityOperationsTable)"
bankBalancesTable="$(bankBalancesTable)"
moneyMarketTable="$(moneyMarketTable)"
ccMovementsTable="$(ccMovementsTable)"
masterHomologationTable="$(masterHomologationTable)"
multicashMovementsTable="$(multicashMovementsTable)"
umbrellaMasterTable="$(umbrellaMasterTable)"
trmTable="$(trmTable)"
renewalOperationsTable="$(renewalOperationsTable)"
operationsATable="$(operationsATable)"
operationsBTable="$(operationsBTable)"
standardizedDerivativesTable="$(standardizedDerivativesTable)"
mambuAccountsTable="$(mambuAccountsTable)"
mambuTransactionsTable="$(mambuTransactionsTable)"
mambuChannelsTable="$(mambuChannelsTable)"
balanceTable="$(balanceTable)"
depositAccountTable="$(depositAccountTable)"
reconciliationAdapterTable="$(reconciliationAdapterTable)"
incomeTable="$(incomeTable)"
expenseTable="$(expenseTable)"
availableTable="$(availableTable)"
summaryTable="$(summaryTable)"
marketDatabase="$(marketDatabase)"
